# UQC-223 — palette stability after window interaction

Status: Planned — diagnosis handoff, 2026-09-18. Initial owner: `holonight-qt`.
No repair assignment or implementation is accepted by this record.

Trigger: [P03 human evidence review](FINDINGS.md#p03-human-evidence-review--2026-09-18).
Appearance-file transitions initially recolor controls, but some later return to
an earlier palette without another scheme change. Main-window New Chat and prompt
buttons reproduce this in both styles. Default Settings native inputs also change
back after the first dark transition; Fusion has a native numeric-control reversal
after the first light transition. User observations associate the changes with
pointer entry, focus/window movement or VT return; causality remains unproven.

Exact baseline: umbrella `65b5fdb`, provider
`e94ceddbd3c0e4cb29e21bcdfd6758e3e4639f4f`, AI
`7e25e78cc7fb33aa63ed480bc3f328a65e04ca0c`, configuration
`fe69a59e6b73167fd5349223a4d265d75386c139`; all other gitlinks remain authoritative.
Reconfirm clean published baselines before assignment. AI is an unchanged reference
consumer, not an additional implementation repository for this package.

Evidence: `.cache/holonight-uqc201-p03-xtndrklf/manual-ai-review-atzox8ft/` contains
an archive made from the user's copied session, replayable `review.py`, `review.json`,
user observations and hashes. Archive SHA-256:
`07ecbfa8fe70944e415341266f0c33298ff16c18d010705a0ada30d807675ccb`.
Default run `ai-1789680270051233025`, Fusion `ai-1789683020270968469`; both measured
DPR 1.25, output scale 1, normal exit 0. `native_timelines` correlates per-instance
roles with the last recorded scheme command; it is not a painted-pixel assertion.

Bounded provider-first diagnosis:

1. Reduce the window palette lifetime boundary using native Button, TextField,
   SpinBox and ComboBox plus appearance-bound composites. Compare default/Fusion,
   existing controls and controls first created in light; include explicit palette
   overrides. Inspect `qml/hnwindowpalette.cpp`, `qml/HnApplicationWindow.qml`,
   `qml/controlpalette.cpp` and platform-theme behavior. AI's
   `qml/workspace/ConversationListPanel.qml` and `qml/shared/ChatComposerActions.qml`
   identify the reported buttons, but do not patch consumer fields speculatively.
2. Distinguish pointer hover, window activation, inherited-palette re-resolution,
   application palette events and VT/session reactivation. Use deterministic
   isolated tests; no live pointer/focus automation. The retained observer lacks
   activation/hover signals and shows equal Active/Inactive role maps in sampled
   palettes, so a color-group explanation alone is not established.
3. Establish a failing reduced regression and final owner before proposing repair.
   Preserve Core isolation, caller overrides/reset, native Fusion behavior, style
   selection and light-time construction. If ownership is outside the provider,
   return a bounded handoff without editing another repository.
4. Record detailed diagnosis and proposed contract in the provider-local SDD only
   when assigned; link it from this ledger. No speculative repair in this review.

After diagnosis and any verified repair, request only affected palette-stability
coverage and the missing fresh Fusion light-time Settings construction. Fusion's
reviewed Settings instance was already visible in dark before the first light
command; hiding/reopening it did not construct a new instance. Preserve successful
transition observations and earlier interaction/navigation coverage. No broad
manual rerun, new kit, privileged procedure or final integration claim is assigned.
P03 open; UQC-222 Done; UQC-201 In Progress; initiative Accepted.
