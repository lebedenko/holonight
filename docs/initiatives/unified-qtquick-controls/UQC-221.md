# UQC-221 — AI Fusion Settings palette ownership follow-up

Status: Done — diagnosis only, 2026-09-17. Confirmed owner: `holonight-qt`
shared-window live palette contract. Published handoff
`30fda50634235a6564c13adcda81fea7ac7212e2`.
[Provider reproduction, measurements and repair contract](../../../holonight-qt/docs/sdd/unified-qtquick-controls/UQC-221.md).
No production repair was implemented; [UQC-222](UQC-222.md) is a Planned proposal.

Trigger: [P03, reviewed AI Sway evidence](FINDINGS.md#batch-8-ai-sway-evidence-review--2026-09-17).
At output scale 1 / measured DPR 1.25, the user accepts editing, navigation,
selectors/popups, scrolling and main-window palette changes in both styles.
During dark → light → dark, the Fusion Settings window retains dark TextField,
numeric-input and ComboBox backgrounds. Do not reopen those passing checks.

Pinned evidence baselines (gitlinks remain authoritative): provider
`7e101bd23661f21efe95cd11dad092069c68601d`, AI
`7e25e78cc7fb33aa63ed480bc3f328a65e04ca0c`, configuration
`fe69a59e6b73167fd5349223a4d265d75386c139`. Reconfirm clean published baselines
before assignment; do not adopt newer revisions automatically.

The initial diagnostic package belongs to the shared provider because its
HnApplicationWindow and palette/composite propagation are the first boundary to
reduce. AI remains an unchanged reference consumer. The observed source paths
are `holonight-qt/qml/HnApplicationWindow.qml`,
`holonight-qt/src/platformtheme/holonighttheme.cpp`,
`holonight-qt/qml/controls/HnIconComboBox.qml`, and AI's
`qml/workspace/SettingsWindow.qml` and provider-settings panels.
Plain QtQuick Window inheritance, native control palettes, and explicitly bound
composite palettes differ; this is an ownership hypothesis, not a repair design.

Bounded next work:

1. Use the retained observer traces and isolated appearance files to correlate
   Settings inputs with their actual palette/background objects. Preserve the
   distinction between owned scene colors and application/window palette roles.
2. Reduce to HnApplicationWindow containing native TextField, SpinBox and ComboBox
   plus a shared composite; compare a plain QtQuick Window at both styles and
   DPR 1/1.25 using programmatic configuration changes, without live pointer/focus
   automation. No providers, credentials, requests or authorization flows.
3. Establish whether the missing update belongs to the shared window/palette
   contract, a composite, AI composition, or an external Qt boundary. If ownership
   leaves the provider, return a bounded handoff; do not edit AI from this package.
4. Report a failing regression and narrow proposed contract before repair scope
   is accepted. Preserve native Fusion styling, explicit palettes, Core isolation
   and embedded-default/override contracts. Do not force a style, paint per-field
   workarounds or alter application-wide palette policy speculatively.

Exit: documented reduced reproduction and owner, with a bounded repair proposal
or explicit external/consumer handoff. A repair kit and focused human confirmation
come only after a verified implementation. This record requests no repeat manual
runs, broad acceptance matrix, new kit or privileged procedures. P01/P02, accepted
Weather repair and prior third-party acceptances retain their existing status.


## Diagnostic outcome

Both plain Window and HnApplicationWindow reproduce unchanged native palettes
under appearance-file updates, in both styles at measured DPR 1 and 1.25.
Application/Window palette Base stays dark; the shared window scene color and
HnSearchField/HnIconComboBox palettes/backgrounds update. Retained AI object
correlations agree: native text/numeric fields remain dark; shared ComboBox
backgrounds update. The broad ComboBox report is not a proven composite defect.

Explicit application-palette changes update existing native controls. A separate
provider-free Fusion reference shows newly recreated windows initialize controls
from startup palette values while existing windows retain the current application
palette. Loaded mappings prove no HoloNight libraries or platform theme in that
reference. Keep that external boundary distinct from the provider repair.

Final matrix repeats 18/18 with identical normalized named-object results. Positive
assertion exits 0; file-transition P03 assertion exits 1 (64 control/window cases);
external recreation assertion exits 1 (8 cases). Provider registered palette,
composite, Core and import-policy selection passes 29/29. Provider SDD records
commands, versions, screenshots, failures and per-object results, plus disclosed
lint advisories. No consumer suite or new manual run was requested. UQC-221 exit
criteria are met; P03 stays open and no new kit is released.
