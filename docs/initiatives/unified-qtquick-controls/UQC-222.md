# UQC-222 — shared-window live palette repair proposal

Status: Planned. Repository: `holonight-qt`. No implementation assigned.
Dependency: completed [UQC-221 diagnosis](UQC-221.md).
Proposed baseline: published diagnostic handoff
`30fda50634235a6564c13adcda81fea7ac7212e2`; reconfirm the authoritative gitlink
and create a repair-local SDD before Ready.

The [provider proposal](../../../holonight-qt/docs/sdd/unified-qtquick-controls/UQC-221.md#repair-proposal-uqc-222-not-implemented)
sets the intended boundary: HnApplicationWindow supplies its current engine
appearance palette as a window-scoped default, for existing and newly constructed
native controls in either style. Bind palette roles through a Core-safe bridge;
preserve explicit window/control/group overrides, disabled roles, native Fusion
rendering, shared composites, Core isolation and style override contracts.

Do not force a style, paint per-field backgrounds, or change application-wide
platform palette policy. The plain Qt/Fusion recreated-window discrepancy has a
provider-free reproduction and remains an external boundary. AI needs no source
change for the proven native-input defect; the reported ComboBox scope remains
qualified by the successful shared-composite traces.

Before assignment, settle the minimal Core palette value surface in the local
SDD. Required regressions are listed in the provider proposal: dark/light/dark,
existing/light-recreated windows, both styles/DPRs, local/full/group overrides,
disabled controls and existing process-boundary checks. Preserve the diagnostic's
separate collection and assertion exits. No repair kit or repeat manual run is
part of this proposal. P03 remains open; UQC-201 In Progress; initiative Accepted.
